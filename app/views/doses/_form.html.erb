<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add a Dose", id: "sweet-alert-demo", class: "btn btn-primary"  %>
<% end %>
